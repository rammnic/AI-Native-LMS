1. Опрос пользователя перед генерацией курса - нужно понять бэкграунд, вдруг юзер то компьютером впервые пользуется. (интерактивный режим)
2. Внутри теории в конце добавить кнопку по типу "Проверить усвоение" - ИИ сгенерит тест с вариантами на основе материала урока (от 5 до n вопросов в зависимости от размера урока). После прохождения теста ( > 50 % отвеченно корректно) теория помечается "изученной" , но есть возможность заново пройти тест ( не перезаписывает "успех" ). если не успешно, новый тест, старый не сохраняем
3. Практика пока вообще не реализованна корректно, нужно делать
4. ai_mentor — чат-ментор - изначально должен отображаться в свернутом виде, а не занимать при открытии страницы пол-экрана
5. Настроить invite-code , новые юзеры не могут зарегаться без него (пока закрытый продакшн)
6. Добавить функцию - полная генерация курса - происходит в фоне, у юзера на странице этого курса отображается прогресс генерации
7. Добавить кнопку удаления курса ( удаляется полностью, в том числе из БД)
8. Добавить профилю кликабельность, в профиле можно поменять пароль, отобржаемое имя, (low priority) сбросить прогресс прохождения по курсу, картинку профиля (low priority)
9. Я не уверен, но ai_mentor по-моему на данном этапе не видит контекст страницы урока с которой вызывается, если у пользователя есть вопрос по материалу ему нужно вручную цитировать
10. | GET | /api/v1/progress/:course_id | Прогресс курса | | POST | /api/v1/progress/:node_id/complete | Отметить завершённым | Вообще пока не понимаю где используется в проекте
11. Убрать "Модель для курсов" из хардкода, перенести в env | Отделить модель для ИИ ассистента, также в env
12. "Стриминг: нужно доработать UI для плавного отображения" есть у нас в плане, но где это юзать ??
13. Отдельные пайпы для разной depth_limit - условные блоки в course_outline.json
14. Ограничение по токенам на генерацию вынести в env (если разные , то несколько переменных)

## Статус задач

| # | Задача | Статус |
|---|--------|--------|
| 1 | Опрос пользователя перед генерацией курса | ❌ Не сделано |
| 2 | Кнопка "Проверить усвоение" с тестами | ❌ Не сделано |
| 3 | Практика — не реализована | ❌ Не сделано |
| 4 | ai_mentor свёрнут по умолчанию | ✅ Сделано |
| 5 | Invite-code для регистрации | ✅ Сделано |
| 6 | Фоновая генерация курса с прогрессом | ❌ Не сделано |
| 7 | Кнопка удаления курса | ✅ Сделано |
| 8 | Кликабельный профиль | ✅ Сделано (страница /profile) |
| 9 | ai_mentor видит контекст урока | ✅ Сделано |
| 10 | API прогресса — интегрировано | ✅ Сделано |
| 11 | Модели ИИ в env | ✅ Сделано |
| 12 | Стриминг UI | ❌ Не сделано |
| 13 | Отдельные пайпы для depth_limit | ✅ Сделано (условные блоки Jinja2) |
| 14 | Токены в env | ✅ Сделано |

## Исправления 23.03.2026

### 1. Страница профиля (/profile)
- Создана страница `frontend/app/profile/page.tsx`
- Функционал: просмотр данных, изменение имени, смена пароля, выход

### 2. Исправлен markComplete API
- Убран лишний `course_id` из тела запроса в `frontend/lib/api.ts`
- Backend сам получает course_id из node

### 3. Токены в env
Добавлены переменные в `backend/.env.example`:
```env
AI_MAX_TOKENS_COURSE=8192
AI_MAX_TOKENS_THEORY=4096
AI_MAX_TOKENS_PRACTICE=4096
AI_MAX_TOKENS_MENTOR=2048
AI_MAX_TOKENS_CODE_VALIDATOR=1024
```

### 4. course_outline.json рефакторинг (v6.0.0)
- Добавлены условные блоки Jinja2 для depth_limit:
  - `{% if depth_limit == 2 %}` - плоская структура (темы → уроки)
  - `{% elif depth_limit >= 3 %}` - иерархическая структура (темы → подтемы → уроки)
- Добавлена поддержка `max_tokens` из input_data

### 5. Пайплайны обновлены
- course_outline.json: v6.0.0
- lesson_theory.json: добавлена поддержка max_tokens из input_data
- lesson_practice.json: добавлена поддержка max_tokens из input_data